# 3x-ui VPN Service configuration
services:
  3x-ui:
    image: ghcr.io/mhsanaei/3x-ui:latest
    container_name: 3x-ui
    restart: unless-stopped
    ports:
      # Important: The admin panel runs on port 2053 inside the container
      # We map it to 2053 on the host for access via http://localhost:2053/BXv8SI7gBe/
      - 2053:2053  # Map container's 2053 port to host's 2053
      # Expose port for Caddy reverse proxy access
      - 54321:54321  # For Caddy reverse proxy access
    volumes:
      # Mount the db directory for persistence
      - ./db:/etc/x-ui
      # Mount certificates directory
      - ./cert:/root/cert
      # Mount logs directory
      - ./logs:/var/log/x-ui
    environment:
      - XRAY_VMESS_AEAD_FORCED=${XRAY_VMESS_AEAD_FORCED:-false}
      - PANEL_PATH=${PANEL_PATH:-BXv8SI7gBe}
      # Set custom base URL for assets
      - BASE_URL=http://${VPN_DOMAIN:-service.foodshare.club}
      # Force using assets from BASE_URL without port
      - ASSETS_OVERRIDE=true
      # Use HTTP for local development
      - XUI_ENFORCE_HTTPS=false
      - FORCE_HTTPS=false
      - DISABLE_TLS=true
      # Set proper URL protocol
      - URL_PROTOCOL=http
      # Custom credentials
      - XUI_USERNAME=${XUI_USERNAME:-admin}
      - XUI_PASSWORD=${XUI_PASSWORD:-admin}
      # JWT Secret
      - JWT_SECRET=${JWT_SECRET:-change_me_in_production}
      # Domain information
      - VPN_DOMAIN=${VPN_DOMAIN:-service.foodshare.club}
      - ADMIN_EMAIL=${ADMIN_EMAIL:-admin@example.com}
    # Removing the health check as the container doesn't have the necessary commands
    # Will rely on process monitoring instead
    networks:
      - vpn-network

networks:
  vpn-network:
    driver: bridge

# If you want to use named volumes instead of local bind mounts
# volumes:
#   x-ui-data:
#     driver: local 